# Pairs with config/config.jarvis.js — copy to `.env` or symlink.
# npm start: node --env-file-if-exists=.env app.js

NODE_ENV=jarvis

#LLM_BACKEND=openai
#LLM_BASE_URL=https://jarvis.kecso.org/v1
#LLM_MODEL=engineering-gpt
# LLM_API_KEY=...   # if the server requires a bearer token

# temporarily housing the local config in here too
LLM_BACKEND=openai
LLM_BASE_URL=http://100.107.170.46:8000/v1
LLM_MODEL=active-llm
CBACK_LLM_REQUEST_TIMEOUT_MS=0